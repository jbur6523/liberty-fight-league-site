# Codex instructions

- The canonical RWI logo asset is `rwi-logo.svg` at the repository root.
- Use this SVG wherever RWI branding is needed.
- Do not redraw, regenerate, reinterpret, or modify the logo artwork.
- Preserve its aspect ratio and transparency.
- If replacing older logo usage such as `logo.png`, prefer `rwi-logo.svg` unless a raster asset is specifically required.
